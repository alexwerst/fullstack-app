{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "build": "rimraf build && tsc",
    "preserve": "npm run build",
    "serve": "cross-env NODE_ENV=development concurrently \"tsc --watch\" \"nodemon -q build/app.js\"",
    "prestart": "npm run build",
    "start": "cross-env NODE_ENV=production node build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "helmet": "^4.6.0",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^16.11.10",
    "@types/body-parser": "^1.19.2",
    "@types/express": "^4.17.13",
    "concurrently": "^6.4.0",
    "nodemon": "^2.0.15",
    "tslib": "^2.3.1",
    "typescript": "^4.5.2"
  }
}
